//result of comparing alternatives A1 and A2 on criteria C
export enum PairWiseResult
{
  Lose=0, //C(A1) is worse than C(A2)
  Draw=1, //C(A1) is equal to C(A2)
  Win=2   //C(A1) is better than C(A2)  
}
